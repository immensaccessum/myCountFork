{
  "name": "mycount",
  "version": "0.4.0",
  "private": false,
  "description": "Unofficial successor to myCount.org — online countdown timer (rewrite + archive fork)",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/build-seo-pages.mjs",
    "preview": "vite preview",
    "server": "node server/server.mjs",
    "og": "node server/server.mjs",
    "test": "vitest run",
    "deploy": "bash scripts/deploy.sh",
    "dev:legacy": "python3 -m http.server 8081 --directory legacy"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/qrcode": "^1.5.6",
    "sharp": "^0.35.3",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
